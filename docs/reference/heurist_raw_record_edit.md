# Call Heurist Record Edit Directly

Low-level wrapper around `record_edit.php`.

## Usage

``` r
heurist_raw_record_edit(session, body = list())
```

## Arguments

- session:

  A `heurist_session`.

- body:

  Form body sent to the endpoint.

## Value

Parsed JSON response.
