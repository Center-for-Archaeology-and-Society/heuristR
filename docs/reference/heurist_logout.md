# Log Out of Heurist

Logs out an authenticated `heurist_session`.

## Usage

``` r
heurist_logout(session)
```

## Arguments

- session:

  A `heurist_session`.

## Value

The updated `heurist_session`.
