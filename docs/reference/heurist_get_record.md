# Get a Single Heurist Record

Fetches one record by record ID.

## Usage

``` r
heurist_get_record(session, record_id)
```

## Arguments

- session:

  A `heurist_session`.

- record_id:

  Numeric or character record ID.

## Value

A parsed Heurist record payload.
