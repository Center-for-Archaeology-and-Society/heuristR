# Call Heurist Record Output Directly

Low-level wrapper around `record_output.php`.

## Usage

``` r
heurist_raw_record_output(session, query = list())
```

## Arguments

- session:

  A `heurist_session`.

- query:

  Query parameters passed to the endpoint.

## Value

Parsed JSON response.
